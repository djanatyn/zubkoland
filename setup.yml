- hosts: localhost
  connection: local # we need this for fast file io
  gather_facts: no
  sudo: yes
  roles:
  - { role: common }
  - { role: ansible-users, users_default_shell: /bin/zsh }
  - { role: ansible }
  - { role: dotfiles }
